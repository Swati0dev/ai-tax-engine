# Task List

Current active tasks for the project.

- [x] Setup `tools/spec-kit`.
- [x] Initialize `.speckit` configuration files.
- [/] UI Refinement: Update `globals.css`.
- [ ] UI Refinement: Create `Breadcrumbs` and `PageHero`.
- [ ] UI Refinement: Add `ReadingProgressBar`.
